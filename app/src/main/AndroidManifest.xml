<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.xgs.androiddemo">

    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".chapter1.RecyclerActivity">
        </activity>
        <activity android:name=".chapter1.CardViewActivity">
        </activity>
        <activity android:name=".chapter1.NotificationActivity">
        </activity>
        <activity android:name=".chapter1.ToolBarActivity">
        </activity>
        <activity android:name=".chapter1.PermissionsActivity">
        </activity>
        <activity android:name=".design.DesignActivity">
        </activity>
        <activity android:name=".design.TabActivity">
        </activity>
        <activity android:name=".view.ViewActivity">
        </activity>
    </application>

</manifest>